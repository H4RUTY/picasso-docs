---
title: アクティビティ図
---

<Mermaid
  chart="
graph TD;
subgraph CC [終了]
  End1(AIの勝ち);
  End2(ユーザの勝ち);
  End3(引き分け);
end
subgraph BB [サーバ]
  F[紙をスライド] --> G[写真撮影];
  G --> H[Geminiで写真を解析し単語を推測];
  H --> I{🔹};
  I --末尾が「ん」でない--> J[ ];
  J --> K[Geminiで次の単語を決める];
  J --> L[紙をスライド];
  K --> M{🔹};
  M --末尾が「ん」でない--> N[Geminiでsvgコードを生成];
  N --> O[svg->Gコード変換してプロット];
  L --> O;
  O --> P[写真撮影...ゲーム履歴UI用];
  R[Geminiでヒントを生成する];
end
subgraph AA [ユーザ]
  A(🟢) --> B[手番を選ぶ];
  B --> C{🔹};
  C --先手を選択--> D[絵を描く];
  D --> E[OKボタンを押す];
  Q{🔹} --ヒントボタンを押さない--> D;
  QUIT[終了ボタンを押す];
end

E --> F;
C --後手を選択--> K;
I --末尾が「ん」--> End1;
M --末尾が「ん」--> End2;
P --> Q;
Q --ヒントボタンを押す--> R;
R --> Q;
QUIT --> End3;
"
/>

<br />

<Callout title="当アクティビティ図のファイル（AI に投げる用）" type="success">
  以下のmermaidコードをコピペすれば、動作について AI に説明する際に便利です。 <br />
  [この図のファイル](https://github.com/H4RUTY/picasso-docs/blob/main/content/docs/developers/activity.mdx?plain=1)
  👈 Click This!!
</Callout>

<br />
